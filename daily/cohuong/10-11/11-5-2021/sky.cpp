#include<bits/stdc++.h>
#define pb push_back
using namespace std;
const int N = 5e4 + 5;

int n, m;
vector<int> adj[N];

int main(){
    cin >> n >> m;
    for (int i = 0; i < m; i ++){
        cin >> b[i] >> p[i];
        cont[b[i]].pb(i);
    }

    for (int i = 0; i < n; i ++){
        for (int u : cont[i]){

        }
    }
}
