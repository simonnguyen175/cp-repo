#include<bits/stdc++.h>
using namespace std;
random_device dev;
mt19937_64 rng(chrono::steady_clock().now().time_since_epoch().count());


int main(){
	ios::sync_with_stdio(0);
	cin.tie.tie(0);cout.tie(0);
	
	n = range(1, 20);
	cout << n;
	for (int i = 1; i <= n; i ++) 
		a[i] = range(1, 20);
	
}
