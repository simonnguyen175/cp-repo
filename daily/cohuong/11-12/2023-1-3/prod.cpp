#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);

    cin >> k >> m >> n;
    for (int i = 1; i <= k; i ++)
    for (int j = 1; j <= m; j ++)
    for (int k = 1; k <= n; k ++)
        cin >> a[i][j][k];

    for (int x = 1; x <= m; x ++)
    for (int u = x + 1; u <= m; u ++)
        for (int y = 1; y <= n; y ++)
        for (int v = y + 1; v <= n; v ++){

            for (int i = 1; i <= k; i ++)

        }

    return 0;
}
