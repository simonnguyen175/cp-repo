#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);

    cin >> l >> r;
    for (int i = 1; i <= r; i ++){
        int n, len;
        cin >> n;
        for (int j = 1; j <= n; j ++){
            cin >> len;
            woods[i].pb(len);
        }
    }

    float cur = 0.5;
    for (int )
}
