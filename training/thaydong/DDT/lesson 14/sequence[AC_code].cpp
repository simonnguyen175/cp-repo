#include<bits/stdc++.h>
using namespace std;

void solve(int n, int x){

for (int i = 1; i <= n; i ++){
        if ( x == 9 ){
            x = 0;
        }
    }
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);

    cin >> n;
    for (int i = 1; i <= n; i ++)
        cin >> a[i];

    for (int i = 0; i <= 9; i ++)
        solve(n, i);
}
