#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);

    cin >> n >> m;
    for (int i = 1; i <= 3; i ++)
    for (int j = 1; j <= 3; j ++){
        int x;
        cin >> x;
        if ( x ) g[i].pb(j);
    }

    a = 'A', b = 'B', c = 'C';
    if ( g[1][3] && g[]

    return 0;
}
