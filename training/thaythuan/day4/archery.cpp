#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);

    cin >> n;
    for (int i = 1, x; i <= n; i ++){
        cin >> type;
        if ( type == 1 ){
            cin >> a[i].x >> a[i].y;
            s.insert({a[i].x+a[i].y, i});
        }
        if
    }
}
