#include<bits/stdc++.h>
using namespace std;

int main(){
    cin >> n >> m;
    for (int i = 1; i <= n; i ++)
        cin >> a[i];

    deque<int> dq;
    for (int i = 1; i <= n; i ++){
        while ( dq.size() && dq.back)
    }

}
